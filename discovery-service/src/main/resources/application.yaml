server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  client:
    # this IS the Eureka server, so do not register itself or fetch the registry
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: ${EUREKA_HOSTNAME:localhost}
