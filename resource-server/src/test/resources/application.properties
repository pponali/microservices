# Dummy issuer URI — satisfies @Value injection in SecurityConfig during @WebMvcTest.
# The JwtDecoder bean is replaced by @MockBean so no actual OIDC metadata fetch occurs.
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:0/auth
spring.main.allow-circular-references=true
